\section{绪论}

\subsection{研究背景}

\subsubsection{鞅与可选停止定理}

鞅（martingale）是概率论中最重要的概念之一，最早由 Paul L\'{e}vy 于 1934 年引入，后经 Joseph L. Doob 在 20 世纪 40--50 年代系统发展，成为现代随机过程理论的基石。鞅刻画了直观上的``公平博弈''：设一个赌徒在第 $n$ 局结束后的财富为 $X_n$，若赌局公平，则在已知前 $n$ 局全部信息 $\mathcal{F}_n$ 的条件下，下一局后财富的条件期望应等于当前财富：
\begin{equation}
  \mathbb{E}[X_{n+1} \mid \mathcal{F}_n] = X_n.
\end{equation}
满足这一性质的随机过程即为鞅。需要强调的是，鞅是一类非常广泛的随机过程：独立同分布零均值随机变量的部分和、布朗运动、补偿泊松过程、Doob 鞅（$\mathbb{E}[X \mid \mathcal{F}_t]$ 的形式）等均为鞅。

可选停止定理（Optional Stopping Theorem，OST）是鞅论中最深刻的结果之一，由 Doob 在其 1953 年的经典著作中首次完整给出\cite{doob1953stochastic}。OST 的核心陈述是：在适当的正则条件下，即使在一个随机停时 $\tau$ 处停止鞅过程，其期望仍然不变：
\begin{equation}
  \mathbb{E}[X_\tau] = \mathbb{E}[X_0].
\end{equation}
这一结论具有广泛的应用：从赌徒破产问题\cite{qian2021randomprocess}到序贯假设检验（Wald 的序贯概率比检验）、从种群遗传学中的固定概率\cite{moran1958random}到保险破产理论\cite{asmussen2010ruin}，OST 提供了将这些看似不同的问题统一在同一数学框架下的工具。

然而，OST 并非无条件成立。Doob 给出了三个充分条件中的任何一个都可保证 OST 成立：（1）停时 $\tau$ 几乎必然有界；（2）在停时前的整个过程 $|X_{t \wedge \tau}|$ 几乎必然有界；（3）$\mathbb{E}[\tau] < \infty$ 且增量 $|X_t - X_{t-1}|$ 一致有界。当这三个条件全部不满足时，OST 可能失效——对称随机游走在单边首达时 $\tau = \inf\{n: S_n = +1\}$ 处的行为是这一失效的经典反例：$\tau$ 几乎必然有限，但 $\mathbb{E}[S_\tau] = 1 \neq 0 = \mathbb{E}[S_0]$。失效的深层原因在于一致可积性的缺失：虽然 $\tau < \infty$ 几乎必然成立，但 $\mathbb{E}[\tau] = \infty$；被停过程 $\{X_{n \wedge \tau}\}$ 在事件 $\{\tau > n\}$ 上的路径可以长时间停留在负半轴并累积任意大的负值，使得该族随机变量无法满足一致可积性条件 $\lim_{M \to \infty} \sup_n \mathbb{E}[|X_{n \wedge \tau}| \mathbf{1}_{\{|X_{n \wedge \tau}| > M\}}] = 0$，从而极限与期望的交换缺乏数学依据。

近年来，随着计算机性能的提升，Monte Carlo 数值模拟为研究 OST 在不同条件下的行为提供了强有力的工具。通过生成大量独立样本路径，可以以高精度估计 $\mathbb{E}[X_\tau]$，并与理论预测进行对比。更重要的是，数值模拟可以直观地展示 OST 失效的微观机制——例如单边停时下路径可以``先任意深地走负再回来''——这是纯解析推导难以传达的洞见。

\subsubsection{数值实验的模型选择}

本文选取三个具有代表性的随机过程模型进行数值实验，它们分别来自种群遗传学、精算科学和经典概率论三个不同的应用领域。这些模型在停时问题的难度上构成了一个由浅入深的递进层次：先从 OST 无条件成立的简单情形入手，逐步过渡到需要构造辅助鞅的情形，再到 OST 条件完全失效的反例。

\smallskip
\noindent\textbf{Moran 种群遗传过程。}\quad 种群遗传学的一个基本问题是：在没有自然选择、突变和迁移的情况下，有限种群中 A 等位基因的频率将如何随时间演化？Moran（1958）提出了一个简单的随机模型来刻画这一过程：每代随机选择一个个体繁殖、随机选择一个个体死亡，A 等位基因频率在代际间发生无偏的随机波动——即遗传漂变（genetic drift）。该模型的自然状态空间有限（$\{0, 1/N, \dots, 1\}$），边界 0 和 1 均为吸收态（分别对应 a固定与 A固定）。由于其过程天然有界，OST 可以直接应用且给出精确等式，因而作为本文数值实验的基准模型，用以验证基本 Monte Carlo 方法的可靠性。

\smallskip
\noindent\textbf{Cram\'{e}r-Lundberg 保险破产模型。}\quad 保险公司的核心风险在于：即使保费定价在长期平均上覆盖了期望赔付，一系列不利的随机索赔事件仍可能导致公司资本耗尽。Cram\'{e}r 和 Lundberg 在 20 世纪初期提出的盈余过程模型至今仍是破产理论的基石：保费以恒定速率连续流入，索赔按泊松过程随机到达，盈余过程表现为线性上升与随机跳跃下跌的叠加。该模型的关键技术挑战在于盈余过程本身并非鞅（具有正向期望漂移），不能直接应用 OST，而需要通过指数变换构造辅助鞅以导出破产概率的指数上界。这一构造性技巧是 OST 在非鞅过程中应用的范例。

\smallskip
\noindent\textbf{赌徒破产模型（单边首达时）。}\quad 赌徒破产问题是概率论中最古老的随机过程模型之一：两个赌徒进行公平博弈，每局输赢一元，直到一方输光。当双方均有资金上限时（双边障碍），游戏以概率 1 在有限期望时间内结束；但当一方拥有无限资本时（单边障碍），虽然游戏仍以概率 1 结束，其期望时间却发散至无穷。这一看似微小的变化——从双边障碍变为单边障碍——使得 OST 的三个充分条件全部崩溃。该模型恰好与保险破产模型构成对比：保险破产模型中，盈余过程本身不是鞅，但通过指数变换构造辅助鞅后 OST 成立；而赌徒破产中，过程本身是鞅，OST 却因停时选择不当（单边障碍）而失效。这两个案例从正反两面说明了 OST 适用性对停时结构的敏感依赖。

\subsection{理论准备}

\subsubsection{鞅与停时}

设 $(\Omega, \mathcal{F}, \mathbb{P})$ 为概率空间，$\{\mathcal{F}_t\}_{t \in \mathbb{T}}$ 为单调递增的 $\sigma$-代数流（即信息流）。$\mathbb{T}$ 为时间参数空间，可以是 $\mathbb{N} = \{0,1,2,\dots\}$（离散时间）或 $[0, \infty)$（连续时间）。

\begin{definition}[鞅]\label{def:mg}
称 $\{\mathcal{F}_t\}$-适应过程 $\{X_t\}_{t \in \mathbb{T}}$ 为鞅（martingale），若：
\begin{enumerate}[noitemsep]
  \item $\mathbb{E}[|X_t|] < \infty$ 对所有 $t \in \mathbb{T}$ 成立；
  \item 对任意 $s < t$，$\mathbb{E}[X_t \mid \mathcal{F}_s] = X_s$（几乎必然）。
\end{enumerate}
若 $\mathbb{E}[X_t \mid \mathcal{F}_s] \geqslant X_s$，则称为下鞅（submartingale）；若 $\leqslant$，则称为上鞅（supermartingale）。
\end{definition}

对于离散时间 $\mathbb{T} = \mathbb{N}$，鞅性质等价于对任意 $n \geqslant 0$ 有 $\mathbb{E}[X_{n+1} \mid \mathcal{F}_n] = X_n$。常见的鞅例子包括：
\begin{itemize}[noitemsep]
  \item 独立零均值随机变量的部分和：$S_n = \sum_{k=1}^{n} \xi_k$，$\mathbb{E}[\xi_k] = 0$；
  \item 独立增量平方的补偿和：$S_n^2 - \sum_{k=1}^{n} \mathbb{E}[\xi_k^2]$；
  \item Doob 鞅：$X_t = \mathbb{E}[X \mid \mathcal{F}_t]$，其中 $X$ 为可积随机变量；
  \item Wald 鞅：$W_n = (\phi(\lambda))^{-n} \exp\{\lambda S_n\}$，其中 $\phi(\lambda) = \mathbb{E}[e^{\lambda \xi_1}]$；
  \item 布朗运动相关鞅：$B_t$，$B_t^2 - t$，$\exp(\lambda B_t - \frac{1}{2}\lambda^2 t)$。
\end{itemize}

\begin{definition}[停时]\label{def:stopping}
取值于 $\mathbb{T} \cup \{+\infty\}$ 的随机变量 $\tau$ 称为 $\{\mathcal{F}_t\}$-停时（stopping time），若对任意 $t \in \mathbb{T}$，$\{\tau \leqslant t\} \in \mathcal{F}_t$。即``是否已经停止''这一事件仅依赖于到当前时刻为止的信息。
\end{definition}

停时的典型例子包括：$\tau \equiv t_0$（固定时刻）、$\tau = \inf\{t: X_t \geqslant a\}$（首达时）、$\tau = \inf\{t: X_t \notin (a, b)\}$（首出时）。对停时 $\tau$，定义被停过程（stopped process）$X_t^\tau = X_{t \wedge \tau}$。

\subsubsection{可选停止定理及其反例}

\begin{theorem}[可选停止定理 --- Doob]\label{thm:ost}
设 $\{X_t\}$ 为鞅，$\tau$ 为停时。若以下三个条件之一成立，则 $\mathbb{E}[X_\tau] = \mathbb{E}[X_0]$：
\begin{enumerate}[noitemsep]
  \item $\tau$ 几乎必然有界，即存在常数 $K$ 使 $\mathbb{P}(\tau \leqslant K) = 1$；
  \item $\{X_{t \wedge \tau}\}$ 有界，即存在常数 $M$ 使 $|X_{t \wedge \tau}| \leqslant M$ 几乎必然对所有 $t$ 成立；
  \item $\mathbb{E}[\tau] < \infty$，且存在常数 $C$ 使 $\mathbb{E}[|X_{t+1} - X_t| \mid \mathcal{F}_t] \leqslant C$ 在 $\{\tau > t\}$ 上成立。
\end{enumerate}
\end{theorem}

在给出证明之前，先回顾一个处理停时期望的基本技巧。对任意停时 $\tau$ 和固定的 $n$，利用示性函数（indicator function）可将概率空间划分为``在 $n$ 之前已停止''和``到 $n$ 时刻仍未停止''两个互斥部分：
\begin{equation}
  \sum_{k=0}^{n-1} \mathbf{1}_{\{\tau = k\}} + \mathbf{1}_{\{\tau \geqslant n\}} = 1.
\end{equation}
由此，被停过程 $X_{\tau \wedge n}$ 的期望可以表达为各互斥事件上条件期望的加权和，从而将随机停时处的期望转化为一系列固定时刻期望的组合。这一技巧在鞅论中反复出现，以下 OST 的证明即为其典型应用。

\textbf{证明（离散时间情形，条件 1）。} 设 $\tau \leqslant K$ 几乎必然。利用上述指示分解：
\begin{align}
  \mathbb{E}[X_{\tau \wedge n}]
  &= \mathbb{E}\Bigg[X_{\tau \wedge n} \Bigg(\sum_{k=0}^{n-1} \mathbf{1}_{\{\tau = k\}} + \mathbf{1}_{\{\tau \geqslant n\}}\Bigg)\Bigg] \\
  &= \sum_{k=0}^{n-1} \mathbb{E}[X_k \mathbf{1}_{\{\tau = k\}}] + \mathbb{E}[X_n \mathbf{1}_{\{\tau \geqslant n\}}].
\end{align}
由于 $\{\tau = k\} \in \mathcal{F}_k$ 且 $\{X_n\}$ 为鞅，$\mathbb{E}[X_n \mid \mathcal{F}_k] = X_k$，故对 $k < n$ 有 $\mathbb{E}[X_n \mathbf{1}_{\{\tau = k\}}] = \mathbb{E}[X_k \mathbf{1}_{\{\tau = k\}}]$。代入得
\begin{equation}
  \mathbb{E}[X_{\tau \wedge n}]
  = \mathbb{E}\Bigg[X_n\Bigg(\sum_{k=0}^{n-1}\mathbf{1}_{\{\tau = k\}} + \mathbf{1}_{\{\tau \geqslant n\}}\Bigg)\Bigg]
  = \mathbb{E}[X_n].
\end{equation}
当 $n$ 很大时 $X_{\tau \wedge n} = X_\tau$（因 $\tau \leqslant K$），取 $n \geqslant K$ 即得 $\mathbb{E}[X_\tau] = \mathbb{E}[X_n] = \mathbb{E}[X_0]$。\hfill\rule{0.9ex}{0.9ex}

条件 2 和 3 的证明分别依赖有界收敛定理和 Wald 引理。这三个条件本质上共同保证了一致可积性——即极限与期望可以交换：
\begin{equation}
  \lim_{n \to \infty} \mathbb{E}[X_{\tau \wedge n}] = \mathbb{E}\Big[\lim_{n \to \infty} X_{\tau \wedge n}\Big] = \mathbb{E}[X_\tau].
\end{equation}
当一致可积性缺失时，被停过程的极限虽然几乎必然存在，但``概率质量''在未停止的路径上可能集中于极端值，破坏期望等式。

为具体说明三个条件全部失效时的后果，考虑一个经典的 OST 反例。令 $S_n = \sum_{i=1}^{n} \eta_i$ 为对称随机游走，$\eta_i = \pm 1$ 各概率 $1/2$，$S_0 = 0$。定义停时 $\tau = \inf\{n: S_n = +1\}$。一维随机游走的常返性保证 $\tau < \infty$ 几乎必然。然而：
\begin{itemize}[noitemsep]
  \item $\tau$ 无界：对任意 $K$，存在正概率使 $\tau > K$——游走可以在达到 $+1$ 之前长时间在负半轴游荡。
  \item $S_{n \wedge \tau}$ 无界：在 $\tau$ 之前，$S_n$ 可以取任意大的负值。
  \item $\mathbb{E}[\tau] = \infty$：首达时期望为无穷（因停时分布的重尾性，$\mathbb{P}(\tau > n) \sim \sqrt{2/(\pi n)}$）\cite{feller1968introduction}。
\end{itemize}
因此三个充分条件全部失效。事实上 $S_\tau \equiv 1$ 以概率 1 成立，故 $\mathbb{E}[S_\tau] = 1 \neq 0 = \mathbb{E}[S_0]$。这一反例正是本文第四章数值研究的核心，也是理解 OST 适用边界的关键入口。

\subsection{数值模拟方法论}

本文统一采用 Monte Carlo 方法进行数值实验。对每个模型，基本流程如下：
\begin{enumerate}[noitemsep]
  \item 对模型参数进行设置（包括初始状态、边界条件等）；
  \item 独立生成 $M$ 条样本路径，每条路径模拟至停时触发或达到预设的最大步数/时间；
  \item 记录每条路径的停时 $\tau^{(i)}$ 和停时处的过程值 $X_{\tau^{(i)}}$；
  \item 计算 $\mathbb{E}[X_\tau]$ 的 Monte Carlo 估计 $\hat{\mu} = \frac{1}{M} \sum_{i=1}^{M} X_{\tau^{(i)}}$；
  \item 所有 Monte Carlo 估计均附有 95\% 置信区间（$\pm 1.96 \times \mathrm{SE}$），各章根据估计量类型分别说明其 SE 的计算方式。
\end{enumerate}

对于需要参数扫描的实验（如破产概率随初始资本的变化），对每个参数值重复上述过程。所有代码以 Python 3.13 编写，数值计算依赖 NumPy 和 SciPy，图形绘制使用 Matplotlib。

在代码架构上，项目采用面向对象的模块化设计，分为公共核心层（\texttt{src/core/}）与各章实验层（\texttt{src/ch02\_moran/} 至 \texttt{src/ch04\_ruin/}）两级结构，文件组织如下图~\ref{fig:code_tree} 所示。核心层提供可复用的基础组件：\texttt{processes.py}（鞅过程抽象基类及具体实现）、\texttt{stopping\_times.py}（停时抽象基类及常用子类）、\texttt{simulation.py}（通用 Monte Carlo 模拟引擎）、\texttt{batching.py}（批次估计）和 \texttt{visualization.py}（统一绘图风格）。实验层整体遵循相近的模块分工：第二、三章包含模型定义文件、实验脚本和绘图脚本；第四章则直接复用 \texttt{core/processes.py} 中的对称随机游走实现，因此目录中仅保留实验脚本与绘图脚本。整套架构使得新增模型的实验通常只需编写模型定义与参数配置，无需重复实现模拟逻辑。

\begin{figure}[H]
  \centering
  \begin{forest}
    for tree={
      font=\ttfamily\zihao{5},
      edge=semithick,
      s sep=2pt,
      inner sep=2pt,
      l sep=8pt,
    }
    [\textrm{src/}
      [\textrm{core/}
        [\textrm{processes.py}]
        [\textrm{stopping\_times.py}]
        [\textrm{simulation.py}]
        [\textrm{batching.py}]
        [\textrm{visualization.py}]
      ]
      [\textrm{ch02\_moran/}
        [\textrm{moran\_model.py}]
        [\textrm{experiment.py}]
        [\textrm{plot.py}]
      ]
      [\textrm{ch03\_insurance/}
        [\textrm{surplus\_model.py}]
        [\textrm{experiment.py}]
        [\textrm{plot.py}]
      ]
      [\textrm{ch04\_ruin/}
        [\textrm{experiment.py}]
        [\textrm{plot.py}]
      ]
    ]
  \end{forest}
  \caption{项目代码文件架构。核心层（\texttt{src/core/}）提供可复用的鞅过程、停时和模拟引擎；第二、三章的实验层包含模型定义、实验脚本和绘图脚本，第四章则直接复用核心层中的对称随机游走实现。}
  \label{fig:code_tree}
\end{figure}
