\section{Moran 模型中的固定概率与停时}

\subsection{背景介绍}

种群遗传学的一个核心问题是：在一个有限种群中，A 等位基因的频率如何随时间演化？在缺乏自然选择、突变和迁移的情况下，A 等位基因频率的演化完全由随机性驱动——每代中哪个个体繁殖、哪个个体死亡都是随机事件。这种现象被称为遗传漂变（genetic drift）。Moran（1958）提出的模型是研究漂变最简单的数学框架之一\cite{moran1958random}。种群遗传学中此类出生-死亡链的更系统处理可参见 Ewens（2004）\cite{ewens2004mathematical}。

考虑一个包含 $N$ 个单倍体个体的种群（单倍体意味着每个个体只携带一个等位基因拷贝，为 A 或 a）。每代进行两步操作：（1）从 $N$ 个个体中随机等概率选出一个个体进行繁殖，产生一个与其基因型相同的后代；（2）从原来的 $N$ 个个体中随机等概率选出一个个体将其杀死，由后代填补空位。令 $X_n$ 表示第 $n$ 步后 A 等位基因的个数，则 $X_n$ 取值为 $\{0, 1, \dots, N\}$。当 $X_n = 0$ 时，种群中仅剩 a 等位基因（记为 a固定）；当 $X_n = N$ 时，种群中仅剩 A 等位基因（记为 A固定）。0 和 $N$ 为吸收态——一旦进入便无法离开。

\subsection{理论分析}\label{sec:moran_theory}

\subsubsection{$X_n$ 的 Markov 链结构与鞅性质}

首先分析 $X_n$ 的一步转移概率。若当前有 $i$ 个 A 型个体（$0 < i < N$），则：
\begin{itemize}[noitemsep]
  \item 繁殖出 A 个体的概率为 $i/N$；
  \item 杀死 A 个体的概率为 $i/N$（被杀死的从原来的 $N$ 个中等概率选出）。
\end{itemize}
当且仅当繁殖出 A 且杀死 a 时（概率 $\frac{i}{N} \cdot \frac{N-i}{N}$）A 的数量增加 1；当且仅当繁殖出 a 且杀死 A 时（概率 $\frac{N-i}{N} \cdot \frac{i}{N}$），A 的数量减少 1。即
\begin{equation}\label{eq:moran_trans}
  P(X_{n+1} = i+1 \mid X_n = i)
  = P(X_{n+1} = i-1 \mid X_n = i)
  = \frac{i(N-i)}{N^2}.
\end{equation}
其余概率为 $P(X_{n+1} = i \mid X_n = i) = 1 - 2i(N-i)/N^2$。因此 $\{X_n\}$ 是一个状态空间为 $\{0,1,\dots,N\}$ 的生灭链，其转移概率矩阵为 $(N+1) \times (N+1)$ 的三对角矩阵：
\begin{equation}\label{eq:moran_P}
  P = \begin{pmatrix}
    1 & 0 & 0 & \cdots & 0 & 0 \\
    p_{1,0} & p_{1,1} & p_{1,2} & \cdots & 0 & 0 \\
    0 & p_{2,1} & p_{2,2} & \cdots & 0 & 0 \\
    \vdots & \vdots & \vdots & \ddots & \vdots & \vdots \\
    0 & 0 & 0 & \cdots & p_{N-1,N-1} & p_{N-1,N} \\
    0 & 0 & 0 & \cdots & 0 & 1
  \end{pmatrix},
\end{equation}
其中 $p_{i,i+1} = p_{i,i-1} = i(N-i)/N^2$，$p_{i,i} = 1 - 2i(N-i)/N^2$（$1 \leqslant i \leqslant N-1$），边界 $p_{0,0} = p_{N,N} = 1$。

由此计算条件期望：
\begin{align}
  \mathbb{E}[X_{n+1} \mid X_n = i]
  &= (i+1) \frac{i(N-i)}{N^2} + (i-1) \frac{i(N-i)}{N^2}
   + i \Big(1 - \frac{2i(N-i)}{N^2}\Big) \notag\\
  &= i + \frac{i(N-i)}{N^2}(+1) + \frac{i(N-i)}{N^2}(-1) = i. \label{eq:moran_mg}
\end{align}
所以 $\mathbb{E}[X_{n+1} \mid \mathcal{F}_n] = X_n$，即 $\{X_n\}$ 是一个鞅。这是非常优美的性质：没有任何选择压力的有限种群中，A 等位基因频率在期望意义下保持不变——尽管在单条轨迹上它必然漂变至吸收。

\subsubsection{OST 与固定概率}

定义停时 $\tau = \inf\{n \geqslant 0: X_n = 0 \ \text{或}\ X_n = N\}$。由于 $X_n$ 是有限状态不可约 Markov 链（除去两个吸收态），$\tau$ 几乎必然有限。由于 $|X_{n \wedge \tau}| \leqslant N$（过程始终在吸收边界内），OST 的条件 2（过程有界）成立，因此
\begin{equation}\label{eq:moran_ost}
  \mathbb{E}[X_\tau] = \mathbb{E}[X_0] = x_0.
\end{equation}
$X_\tau$ 只能取两个值：0（a固定）或 $N$（A固定）。设 $P_N = \mathbb{P}(X_\tau = N \mid X_0 = x_0)$ 为固定概率，则
\begin{equation}
  \mathbb{E}[X_\tau] = 0 \cdot (1 - P_N) + N \cdot P_N = N P_N.
\end{equation}
联立式 \eqref{eq:moran_ost} 得 $N P_N = x_0$，即
\begin{equation}\label{eq:moran_fix}
  P(\text{A固定}) = \frac{x_0}{N}.
\end{equation}
这是 OST 在这一模型中给出的最简洁且深刻的结论：\textbf{A 等位基因的初始频率即固定概率}。假如初始时 A 等位基因频率为 30\%，则其最终在种群中固定的概率就是 30\%。没有任何选择、突变或迁移时，所有等位基因的命运在期望意义上由其初始频率唯一决定。

\subsubsection{$\mathbb{E}[\tau]$ 的数值求解}

我们关心从状态 $i$ 出发时的平均吸收时间
\begin{equation}
  t_i = \mathbb{E}[\tau \mid X_0 = i], \qquad i = 0,1,\dots,N.
\end{equation}
对出生-死亡链做一步分析。设当前状态为 $i$（其中 $1 \leqslant i \leqslant N-1$），则下一步可能上升、下降或保持不变，于是
\begin{equation}
  t_i
  = 1 + p_{i,i+1} t_{i+1} + p_{i,i-1} t_{i-1}
    + \bigl(1-p_{i,i+1}-p_{i,i-1}\bigr)t_i.
\end{equation}
整理即得三对角线性方程组
\begin{equation}\label{eq:moran_tri}
  p_{i,i-1} t_{i-1} + p_{i,i+1} t_{i+1} - (p_{i,i-1} + p_{i,i+1}) t_i = -1, \quad 1 \leqslant i \leqslant N-1,
\end{equation}
边界条件为 $t_0 = t_N = 0$。写成矩阵形式 $A \mathbf{t} = -\mathbf{1}$：
\begin{equation}\label{eq:moran_matrix}
  \begin{pmatrix}
    -2p_1 & p_1 & 0 & \cdots & 0 \\
    p_2 & -2p_2 & p_2 & \cdots & 0 \\
    0 & p_3 & -2p_3 & \cdots & 0 \\
    \vdots & \vdots & \vdots & \ddots & \vdots \\
    0 & 0 & 0 & \cdots & -2p_{N-1}
  \end{pmatrix}
  \begin{pmatrix} t_1 \\ t_2 \\ t_3 \\ \vdots \\ t_{N-1} \end{pmatrix}
  =
  \begin{pmatrix} -1 \\ -1 \\ -1 \\ \vdots \\ -1 \end{pmatrix},
\end{equation}
其中为简化记号，令 $p_i = p_{i,i+1} = p_{i,i-1} = i(N-i)/N^2$。该三对角系统可通过标准数值线性代数高效求解。后文的数值实验将 Monte Carlo 估计的停时均值与这一理论解进行对比验证。

\subsection{数值实验}

实验在 Python 3.13 环境下运行。实验参数设置如表~\ref{tab:moran_params} 所示。

\begin{table}[ht]
  \centering
  \caption{Moran 模型数值实验参数设置}
  \label{tab:moran_params}
  \begin{tabular}{lll}
    \toprule
    参数 & 实验一：固定概率 & 实验二：停时均值 \\
    \midrule
    种群规模 $N$ & 50 & 50 \\
    初始 A 个数 $x_0$ & $\{5, 10, \dots, 45\}$ & $\{5, 10, \dots, 45\}$ \\
    模拟路径数 $M$ & 50000 $\times$ 9 组 & 50000 $\times$ 9 组 \\
    \bottomrule
  \end{tabular}
\end{table}

样本轨道（图~\ref{fig:ch02_paths}）首先给出了漂变过程的直观印象：在 $N=50$、初始频率为 0.5 的条件下，每条轨迹呈锯齿状随机游走，波幅在频率接近 0.5 时最大（此时 $i(N-i)/N^2$ 达到峰值），靠近吸收边界时逐步减弱。蓝色与红色轨迹分别对应 A 固定与 a 固定的结局；尽管没有选择压力，随机漂变本身足以驱使系统在有限时间内到达两个单等位基因状态之一。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{ch02_paths}
  \caption{Moran 模型基因频率轨迹（$N=50$，A 等位基因初始频率 0.5）。蓝色路径表示最终 A固定，红色路径表示最终 a固定。每条轨迹为锯齿状随机游走，最终被 0 或 1 吸收。圆点标记停时位置。}
  \label{fig:ch02_paths}
\end{figure}

定量层面的核心检验围绕 OST 的两个推论展开。首先是固定概率公式 $\mathbb{P}(\text{A固定}) = x_0/N$。对九个初始频率各模拟 50000 条独立路径，估计值与理论对角线的高度吻合（图~\ref{fig:ch02_fixation} 上图），残差面板（下图）显示偏离幅度很小且不呈现系统性模式，与 OST 在过程有界条件下的精确成立预期一致。其次是期望停时。第~\ref{sec:moran_theory}~节从出生-死亡链的一步分析导出了三对角系统~\eqref{eq:moran_matrix}；将 Monte Carlo 停时均值与该系统的解进行对比（图~\ref{fig:ch02_tau_sim}），上图可见估计值与理论曲线高度吻合，下图残差在所有初值下均与零线一致。两组检验分别从固定概率和吸收时间两个维度确认了理论推导的正确性。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{ch02_fixation}
  \caption{固定概率与 A 等位基因初始频率的关系（$N=50$）。上图：红色实线为理论值 $y = x_0/N$，蓝色圆点为 Monte Carlo 估计（50000 条路径）。下图：估计值偏离理论值的残差，误差棒为 95\% 置信区间。}
  \label{fig:ch02_fixation}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{ch02_tau_sim}
  \caption{不同 A 等位基因初始频率下 Monte Carlo 停时均值与理论解的对比（$N=50$）。上图：红色实线为三对角线性系统~\eqref{eq:moran_matrix} 给出的理论值，蓝色圆点为 Monte Carlo 估计（50000 条路径）。下图：估计值偏离理论值的残差，误差棒为 95\% 置信区间。}
  \label{fig:ch02_tau_sim}
\end{figure}

从 $x_0 = 25$ 出发的期望停时约为 1700--1800 步——在进化时间尺度上，这相当于约 $35$ 代（$N=50$ 个个体平均更新一次为一代），即使没有自然选择，随机漂变也能在几十代内完成等位基因的固定。
