\section{美式期权定价与 Longstaff-Schwartz 算法}

\subsection{背景介绍}

期权定价是金融数学的核心问题，而其数学本质——特别是在美式期权中——是一个最优停时问题。期权合约的买方支付一笔权利金，以换取在未来某个时间窗口内按约定价格交易标的资产的权利；卖方则收取权利金并承担履约义务。与远期或期货不同，期权的关键特征在于买方可以“选择是否执行”，因此其价值不仅取决于未来价格本身，还取决于决策时点的灵活性。

欧式看跌期权给予持有者在到期日 $T$ 以执行价 $K$ 卖出标的资产的权利，其 payoff 为 $\max(K - S_T, 0)$。这种合约适合那些只关心到期风险暴露的投资者，例如希望在某一确定时点对冲股价下跌风险的持仓者。美式看跌期权则赋予持有者在到期前\textbf{任意时刻}行权的权利，因此其价值必然不低于欧式期权。多出来的这部分价值正是\textbf{提前行权溢价}——即最优停时策略相对于固定行权时间的额外收益。

对看跌期权而言，提前行权何时有利与利率、剩余时间价值和标的价格水平密切相关。当标的价格大幅跌破执行价时，持有者已经处于深度价内状态；若此时继续持有所能获得的额外时间价值有限，而立即行权又可以更早锁定现金流，那么提前行权就可能成为最优选择。也正因为如此，美式看跌期权的定价不再只是一个终值贴现问题，而是一个带自由边界的动态决策问题。

在风险中性定价框架下，美式期权价格由以下最优停时问题给出\cite{longstaff2001valuing}：
\begin{equation}\label{eq:am_option}
  V_0 = \sup_{\tau \in [0, T]} \mathbb{E}^{\mathbb{Q}}\big[e^{-r\tau} g(S_\tau)\big],
\end{equation}
其中 $g(s) = \max(K - s, 0)$ 为看跌期权的 payoff 函数，$\mathbb{Q}$ 为风险中性测度，在 $\mathbb{Q}$ 下标的资产价格服从几何布朗运动：
\begin{equation}\label{eq:gbm}
  dS_t = r S_t dt + \sigma S_t dW_t.
\end{equation}

与秘书问题的离散有限状态空间不同，美式期权的状态空间是连续的且决策时间也是连续的（实践中可离散化为有限个时间步）。这使得 Snell 包络的递推计算无法通过简单的有限状态求和完成，需要借助 Monte Carlo 模拟和回归技术。Longstaff 和 Schwartz 于 2001 年提出的算法\cite{longstaff2001valuing}（简称 LSM）正是解决这一问题的经典方法。

\subsection{理论分析}

\subsubsection{Snell 包络与行权边界}

将时间 $[0, T]$ 离散为 $N$ 步，步长 $\Delta t = T/N$。在离散时间下，美式期权的 Snell 包络递推为
\begin{equation}\label{eq:snell_option}
  V_n(S_n) = \max\Big\{g(S_n),\; e^{-r\Delta t} \cdot \mathbb{E}^{\mathbb{Q}}[V_{n+1}(S_{n+1}) \mid S_n]\Big\},
\end{equation}
其中 $V_N(s) = g(s)$（到期日只能行权或作废）。$e^{-r\Delta t} \mathbb{E}^{\mathbb{Q}}[V_{n+1} \mid S_n]$ 称为继续持有价值。折现过程 $\{e^{-r n\Delta t} V_n\}$ 是 $\mathbb{Q}$-上鞅——这是 OST 的核心推广：上鞅的停止定理保证任意停时下的期望不超过初始值，支持了延续区域中等价、停止区域内行权的最优决策。

最优行权策略由一个\textbf{停时边界} $S^*(t)$ 完全刻画：当 $S_t \leqslant S^*(t)$ 时（即期权深度 in-the-money），立即行权；当 $S_t > S^*(t)$ 时，继续持有。这一边界将 $(t, S)$ 平面划分为\emph{停时区域}和\emph{延续区域}。

\subsubsection{Longstaff-Schwartz 算法}

LSM 算法的核心创新在于用最小二乘回归估计 continuation value。算法流程如下：

\begin{enumerate}
  \item \textbf{前向模拟}：生成 $M$ 条独立的股价路径 $\{S_n^{(i)}\}_{n=0}^{N}$，$i = 1, \dots, M$。由于几何布朗运动有显式解，本文使用保持正性的对数正态精确离散化：
  \begin{equation}\label{eq:em}
    S_{n+1}^{(i)} = S_n^{(i)} \exp\Big\{(r-\tfrac{1}{2}\sigma^2)\Delta t
    + \sigma\sqrt{\Delta t}\, Z_n^{(i)}\Big\},
    \quad Z_n^{(i)} \stackrel{\text{iid}}{\sim} N(0, 1).
  \end{equation}
  \item \textbf{倒向递推}：初始化每条路径的候选现金流为到期收益，并令候选行权时刻 $\tau^{(i)}=N$。随后从 $n = N-1$ 倒推至 $n = 1$：
  \begin{enumerate}
    \item 筛选\emph{价内路径}：保留 $g(S_n^{(i)}) > 0$ 的路径索引集合 $\mathcal{I}_n$。
    \item 将每条路径当前记录的未来现金流折现回 $n$ 时刻：
    \begin{equation}
      Y^{(i)} = e^{-r(\tau^{(i)}-n)\Delta t}\, g(S_{\tau^{(i)}}^{(i)}),
      \quad i\in \mathcal{I}_n.
    \end{equation}
    \item 以当前股价 $X^{(i)} = S_n^{(i)}$（$i \in \mathcal{I}_n$）为自变量，$Y^{(i)}$ 为因变量，做多项式回归（本文使用二阶多项式 $\beta_0 + \beta_1 S + \beta_2 S^2$）：
    \begin{equation}\label{eq:lsm_reg}
      \hat{C}(s) = \hat{\beta}_0 + \hat{\beta}_1 s + \hat{\beta}_2 s^2.
    \end{equation}
    回归系数 $\hat{\beta}$ 通过普通最小二乘法获得。
    \item 比较：若 $g(S_n^{(i)}) > \hat{C}(S_n^{(i)})$，则把该路径的候选行权时刻更新为 $\tau^{(i)}=n$，现金流更新为 $g(S_n^{(i)})$；否则保留原未来行权时刻和现金流。
  \end{enumerate}
  \item \textbf{定价}：美式期权价格的 Monte Carlo 估计为
  \begin{equation}\label{eq:lsm_price}
    \hat{V}_0 = \frac{1}{M} \sum_{i=1}^{M} e^{-r \tau^{(i)} \Delta t} \cdot g(S_{\tau^{(i)}}^{(i)}),
  \end{equation}
  其中 $\tau^{(i)}$ 为第 $i$ 条路径上 LSM 算法判定的最优停时指数。
\end{enumerate}

\textbf{回归基函数的选择}是 LSM 算法的关键设计决策。Longstaff 和 Schwartz 的原始论文指出，仅需少数几个基函数（如常数项、$S$、$S^2$）就能给出良好的近似，因为继续持有价值作为 $S$ 的函数通常较光滑（它与 Black-Scholes 偏微分方程相联系）。本文采用二阶多项式作为基函数。

\textbf{OST 在 LSM 中的结构性角色}需要特别阐明：OST 并不直接用于导出美式期权的定价公式，但它是整个 Snell 包络理论合法性的基石。由递推定义可得
\begin{equation}
  V_n \geqslant
  e^{-r\Delta t}\mathbb{E}[V_{n+1}\mid\mathcal{F}_n],
\end{equation}
因此折现 Snell 包络 $\{e^{-r n\Delta t}V_n\}$ 是一个上鞅。上鞅的停止定理保证任意停时下的折现期望不超过初始值，这正是“比较继续持有价值和立即行权收益”这一决策逻辑的数学依据。

\subsection{数值实验}

实验参数设置如下：初始股价 $S_0 \in [36, 44]$（17 个取值），执行价 $K = 40$，无风险利率 $r = 6\%$，波动率 $\sigma = 20\%$，到期时间 $T = 1$ 年，时间步数 $N = 50$（步长 $\Delta t = 0.02$）。对于图~6.3，每个 $S_0$ 点总共模拟 30000 条路径，并拆分为 8 个独立批次；图中的美式价格箱线图与表格中的均值、标准误都由这同一组批次结果计算得到。欧式看跌期权的基准价格由 Black-Scholes 公式直接计算：
\begin{equation}\label{eq:bs_put}
  P_{\text{Eur}} = K e^{-rT} \Phi(-d_2) - S_0 \Phi(-d_1),
\end{equation}
其中 $d_{1,2} = [\ln(S_0/K) + (r \pm \sigma^2/2)T] / (\sigma\sqrt{T})$，$\Phi$ 为标准正态分布函数。

\begin{table}[H]
  \centering
  \caption{美式期权数值实验条件}
  \label{tab:option_params}
  \begin{tabular}{llll}
    \toprule
    参数 & 图 6.1 & 图 6.2 & 图 6.3 \\
    \midrule
    初始股价 $S_0$ & 40 & 40 & 36--44（17 点） \\
    执行价 $K$ & 40 & 40 & 40 \\
    无风险利率 $r$ & 6\% & 6\% & 6\% \\
    波动率 $\sigma$ & 20\% & 20\% & 20\% \\
    到期时间 $T$ & 1 年 & 1 年 & 1 年 \\
    时间步数 $N$ & 50 & 50 & 50 \\
    路径数 $M$ & 30000（截取 15 条展示） & 30000 & 30000（8 批次） \\
    随机种子 & 42 & 42 & 42 \\
    \bottomrule
  \end{tabular}
\end{table}

以下从样本路径、行权边界和期权价格三个层面进行分析。

图~\ref{fig:ch06_paths} 展示了 15 条几何布朗运动股价路径及 LSM 算法判定的行权时刻。红色圆点表示提前行权，橙色三角表示到期行权；若到期时仍为价外，则没有 payoff，图中不额外标记。可以看到，提前行权集中在股价显著低于执行价 $K = 40$ 的区域（即深度 in-the-money），此时继续等待的时间价值可能小于立即锁定收益的价值。这一区分也说明，美式期权的价值不仅来自到期 payoff，还来自可在路径中途选择停止的权利。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{ch06_paths}
  \caption{几何布朗运动股价路径与 LSM 判定行权时刻（$S_0 = 40, K = 40$）。红色圆点标记提前行权，橙色三角标记到期行权；到期价外且 payoff 为 0 的路径不额外标记。水平虚线为执行价 $K$。}
  \label{fig:ch06_paths}
\end{figure}

\textbf{图~\ref{fig:ch06_boundary}} 以热力图的形式展示了 $(t, S)$ 平面上各位置被 LSM 算法判定为``立即行权''的经验频率。颜色越亮表示行权概率越高，白色等值线标记约 $50\%$ 的行权边界。可以清晰地看到一条分界带：在股价较低的区域（大约 $S \lessapprox 34$--$36$），行权概率高；在股价较高的区域，行权概率低。这条分界线并非垂直的——它在靠近到期日（$t \to 1$）时上移，意味着越接近到期日，持有者越愿意在相对高的股价水平行权（因为剩下的时间价值已经有限）。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{ch06_boundary}
  \caption{美式看跌期权的行权概率热力图。颜色越亮表示该位置被 LSM 判定为立即行权的概率越高，白色等值线对应约 $50\%$ 行权概率。股价低于约 $34$--$36$ 时为停时区域，高于此区间时为延续区域；边界在靠近到期日（$t \to 1$）时向上弯曲，因为剩余时间价值减少。}
  \label{fig:ch06_boundary}
\end{figure}

\textbf{图~\ref{fig:ch06_price}} 比较了美式（LSM Monte Carlo）和欧式（Black-Scholes 公式）看跌期权的价格随初始股价的变化。三条线/数据分别为：（1）美式 LSM 价格的批次分布（蓝色箱线图）——每个 $S_0$ 对应 8 个独立 Monte Carlo 批次；（2）欧式 Black-Scholes 价格（红色实线）；（3）立即行权 payoff $\max(K - S_0, 0)$（灰色虚线）——期权内在价值的下界。由于图中的箱线图和表中的均值都来自同一组批次，图表和统计口径保持了一致。

美式期权价格在所有 $S_0$ 取值下均不低于欧式期权价格——两者之差即为\emph{提前行权溢价}（early exercise premium）。在 deep in-the-money 区域（$S_0 \leqslant 37$），美式和欧式期权价格都接近 payoff 曲线，因为此时几乎可以确定期权会在到期日被执行，提前行权的额外价值相对较小。在 at-the-money 附近（$S_0 \approx 40$），美式期权的溢价最大（约 $2.32$ vs $2.07$，溢价约 12\%），因为此时不确定性最高，等待的选择权价值最大，提前行权的灵活性也最有价值。在 deep out-of-the-money 区域（$S_0 \geqslant 43$），两者都趋近于零。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{ch06_price}
  \caption{美式（LSM）与欧式（Black-Scholes）看跌期权价格对比。蓝色箱线图为每个 $S_0$ 下 8 个独立 LSM Monte Carlo 批次的价格分布（总路径数 30000，$N = 50$）；红色实线为欧式 BS 公式。灰色虚线为立即行权 payoff。美式期权在所有 $S_0$ 处均高于欧式期权，差额即为提前行权溢价。}
  \label{fig:ch06_price}
\end{figure}
